Send Mail
Send the email with JS code
    Make a POST request to /emails
    Load user's sent mailboc once mail is Send

Mailbox
Load the apropiate Inbox, Sent mailbox or Archive
    Make a GET request to /emails/<mailbox> to get a particular email
    When a mailbox is visited, the API should give the latest emails in that box
    When a mailbox is visited, the name should appear at the top of the page
    Each email should be render in its own box (from, subject, timestamp)
    If
        Email is read  background gray
    else background white

View Email
Clicking the email should take to a view with the email content
    Make a GET request to /emails/<email_id> to get the email
    Should show the email sender, recipients, subject, timestamp and body
    inbox.html probably needs another div to show the email
    When the email is clicked on, mark it as read

Archive and Unarchive
Users should be able to archive or unarchive emails 
    When viewing an inbox email, a button must allow to archive the email
    if is archive a button must allow to unarchive the email (does not apply to sent emails)
    Once the email is in the archive or unarchive load the user Inbox

Reply
Users must be able tu Reply
    When seeing a email users must see a Reply button
    The form must be prefill with the recipient field (who sent the original email)
    The form must be prefill with the subject field (if is foo the new must be Re: foo, unless already has the Re:)
    The body must be prefill with a line like "On Jan 1 2020, 12:00 AM foo@example.com wrote:" followed by the original text
